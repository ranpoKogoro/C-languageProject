#include <stdio.h>
int main(void){
    // ここに計算式を書いて、
    int number = 1234 * 5678 / 2;
    printf("%d",number);
    int x = 8;
    int y = 5;
    // 以下に、xをyで割ったときの余りを計算し、結果を出力するコードを書いてください
    printf("%d", x % y);
}
